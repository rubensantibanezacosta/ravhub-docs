# Primeros pasos

Este proyecto sirve como base para la documentación de RavHub.

## Desarrollo local

1. Instala dependencias con `pnpm install`.
2. Arranca el sitio con `pnpm docs:dev`.
3. Abre la URL que muestre VitePress.

## Build

Ejecuta `pnpm docs:build` para generar el sitio estático.
